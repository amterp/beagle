# Beagle

Beagle is a macOS job orchestrator CLI for scheduled and always-on jobs.

Users configure jobs in `beagle.yaml` and interact with Beagle commands (`validate`, `apply`, `ls`, `status`, and more). The scheduler backend is an implementation detail.
