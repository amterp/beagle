# Beagle

Beagle is a macOS job orchestrator CLI for scheduled and always-on jobs.

Users configure jobs in `beagle.yaml` and interact with Beagle commands. The scheduler backend is an implementation detail.

See `docs.md` for config and command examples.
